# NLP Suite Python dependencies
# Used by GitHub Actions CI for PyInstaller builds and the bundled portable Python env.
# Authoritative, complete list (reconciled with Evan's package inventory).
# Transitive dependencies (absl-py, gast, numba, llvmlite, jedi, parso, traitlets, ...) are
# pulled automatically and are NOT listed here.
# Platform-specific extras live in requirements-windows.txt and requirements-mac.txt.

# Core data science
pandas
numpy
scipy
scikit-learn
openpyxl
xlrd
numexpr
h5py

# NLP
stanza
spacy
nltk
gensim
textblob
textstat
nrclex
langdetect
langid
autocorrect
pyspellchecker
fuzzywuzzy[speedup]
sacremoses
sentencepiece
editdistance
wn
pywsd
contextualSpellCheck
spacytextblob
spacy-langdetect

# Transformers / deep learning
# transformers pinned <5: transformers 5.x breaks bert-extractive-summarizer 0.10.1 (Evan's testing).
transformers==4.41.2
sentence-transformers
tensorflow
tensorflow-hub
tf-keras
bert-extractive-summarizer

# Coreference resolution (neural engine for the Coreference GUI)
# fastcoref -> the 'BERT' option (SpanBERT/LingMess); its model self-downloads on first use.
# NOTE: coreferee (the former 'spaCy' option) was removed - it pins spacy<3.6, which forces the
# whole suite's spaCy stack down into un-buildable source dists and breaks the PyInstaller build.
fastcoref

# Topic modeling
bertopic
pyLDAvis
umap-learn
hdbscan

# Visualization
matplotlib
plotly
seaborn
wordcloud
mpld3
kaleido

# GIS / Maps
folium
geopy
simplekml
shapely
geopandas
geocoder
googlemaps
geojson
gmaps

# Web / parsing
# FLOORS, not pins: these are unpinned so a fresh install gets the current release, but a floor
# stops a cached wheel or an old resolver from quietly satisfying the requirement with a version
# carrying a known advisory. Raise a floor when an advisory lands; do not turn these into ==.
#   requests  < 2.31.0  leaks the Proxy-Authorization header across redirects (GHSA-j8r2-6x86-q33q)
#             < 2.32.0  a Session keeps not verifying after one verify=False request
#             < 2.32.4  .netrc credentials can leak via a malicious URL (GHSA-9hjg-9r4m-mvj7)
#   urllib3   < 1.26.19 same Proxy-Authorization leak on cross-origin redirects; it arrives as a
#             dependency of requests, so it is named here to keep the floor explicit
# Both matter on a campus proxy, which is where a good share of these users are.
requests>=2.32.4
urllib3>=1.26.19
beautifulsoup4
# lxml deliberately unpinned. The XXE advisory against it is about lxml's DEFAULT parser settings,
# and the Suite no longer relies on those: every place that parses a file somebody else wrote
# passes an explicit hardened parser (IO_files_util.safe_xml_parser). Pinning up to lxml 6 for the
# default alone would risk Gephi_util, which parses .gexf in several places.
lxml
chardet
tqdm
psutil

# PDF / document
# pdfminer.six: pdf --> txt, and the layout (text + image positions) for pdf --> docx
# pypdf: decodes the images embedded in a pdf for the pdf --> docx converter; pdfminer's own
#   ImageWriter fails on ordinary png images ('unrecognized image mode') and what it writes,
#   python-docx then refuses
# pdf2image + pytesseract were dropped with the OCR converter: that menu option never ran
#   (it was wired to pdfminer), and real OCR needs the Tesseract + poppler BINARIES, which the
#   frozen installers do not ship
pdfminer.six
pypdf
python-docx
striprtf

# Stanford CoreNLP client
pycorenlp
stanfordcorenlp

# Image
# Pillow pinned to 10.4.0: Pillow 12.x is incompatible with the bundled Tcl/Tk 8.6 runtime and
# crashes loading the welcome-screen images (Evan's testing) - the app won't open without this pin.
Pillow==10.4.0

# Knowledge graphs
SPARQLWrapper

# GUI
tkcolorpicker

# Auto-update via git
pygit2

# Jupyter widgets (used by pyLDAvis visualizations)
ipython
ipywidgets
